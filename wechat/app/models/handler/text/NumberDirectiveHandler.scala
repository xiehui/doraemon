package models.handler.text

import models.ReplyMsg

class NumberDirectiveHandler extends DirectiveHandler {

  override def hand(directive: String, param: String) : ReplyMsg = {
    null
  }
}